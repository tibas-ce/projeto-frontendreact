export const goToHomePage = (navigate) => {
  navigate("/");
};

export const goToBoardingPalnet = (navigate) => {
  navigate("/boardingPalnet");
};

export const goToLogin = (navigate) => {
  navigate("/login");
};

export const goToSignUp = (navigate) => {
  navigate("/signup");
};
